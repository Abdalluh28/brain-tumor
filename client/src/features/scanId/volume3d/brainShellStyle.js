/** Soft pink-beige tissue tone — visible but still lets tumor show through. */
export const BRAIN_SHELL = {
    color: 0xe8c4b8,
    emissive: 0x9a6b5c,
    emissiveIntensity: 0.12,
    opacity: 0.42,
    hex: "#e8c4b8",
};

export const BRAIN_SHELL_LEGEND_COLOR = BRAIN_SHELL.hex;
